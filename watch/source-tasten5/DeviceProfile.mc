// Geraeteprofil A — fuenf Tasten, kein Touch (fenix6pro, fr945).
// Wird nur fuer diese Geraete kompiliert (monkey.jungle).
module DeviceProfile {
    const HAS_UP_DOWN = true;

    // Bedienhinweise auf den Oberflaechen
    const L_SELECT      = "START";
    const L_SELECT_HOLD = "START halten";
}
