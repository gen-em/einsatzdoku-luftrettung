// Geraeteprofil B — drei Tasten (zwei nutzbar), Touch (venu3s).
// Die mittlere Taste ist systemseitig belegt und erreicht die App nicht,
// siehe docs/Geraete-Eingabe.md. Ohne Touch ist das Geraet unbedienbar.
module DeviceProfile {
    const HAS_UP_DOWN = false;

    // Bedienhinweise auf den Oberflaechen
    const L_SELECT      = "Action";
    const L_SELECT_HOLD = "Action halten";
}
